# PlayAIG Git Baseline Recovery Phase 1.4 Acceptance

**Project:** PlayAIG / SpiritVale  
**Date:** 2026-08-17  
**Scope:** Independent clean reconstruction and reproducibility validation only.

## Executive Summary

An independent reconstruction was created from `origin/main` at `2284785c6ea87c82d3336083c8839eb7e830d20f`. The original dirty worktree was read before and after the exercise and remained at 118 status entries, on `main`, with the same HEAD. The approved runtime manifest was migrated without credentials, then the existing test dependency graph exposed a manifest conflict: 18 additional non-secret acquisition/test files were required for the declared validators to run. Those files are explicitly classified as `UNKNOWN_BUT_REQUIRED`; no broad scripts or production data directory was copied.

The clean reconstruction passed install, typecheck, lint, production build, the complete npm test suite, data parity, route parity, SEO parity, Phase 1 content parity, browser review and a second independent reproducibility build. No commit, push or deployment was performed.

## Clean Reconstruction Location

- `CLEAN_RECONSTRUCTION_PATH=/Users/cj/Documents/PlayAIG-Clean-Reconstruction-20260817/`
- Initial worktree status: clean before migration
- Reconstruction status before Phase 1.4 evidence files: 45 status entries (35 tracked changes plus 24 untracked files grouped by directory); final status after adding the delta manifest and acceptance file: 47
- Source migration: 57 files (39 manifest-listed plus 18 unknown-but-required)
- Generated outputs: 2 (`public/sitemap.xml`, `public/rss.xml`)
- `RECONSTRUCTION_DELTA_FILE_COUNT=59` including generated outputs

## Reconstruction Branch

- `RECONSTRUCTION_BRANCH=recovery/playaig-clean-baseline-20260817`
- Branch was created without checkout of the original worktree.
- Reconstruction HEAD: `2284785c6ea87c82d3336083c8839eb7e830d20f`
- `origin/main`: `2284785c6ea87c82d3336083c8839eb7e830d20f`

## Starting Baseline

`CLEAN_RECONSTRUCTION_HEAD=origin/main=2284785c6ea87c82d3336083c8839eb7e830d20f`

The original project remained at `/Users/cj/Documents/SpiritVale站`, branch `main`, HEAD `2284785c6ea87c82d3336083c8839eb7e830d20f`.

## Manifest Migration

The approved manifest was read from the Phase 1.3 documentation in the original project. Its explicit lists contain 39 files, although the document declares a candidate count of 40. This discrepancy is recorded as:

`MANIFEST_CONFLICT=YES`

The explicit 39 files were copied coherently across runtime/data, build/schema and validation-only groups. Generated sitemap/RSS files were regenerated by the build and were not treated as source-of-truth. Every copied file matched the original worktree SHA-256 (`HASH_MISMATCHES=0`). Full path-level evidence is in:

`docs/recovery/RECONSTRUCTION_DELTA_MANIFEST_V1.md`

## Unknown But Required Files

`UNKNOWN_BUT_REQUIRED_COUNT=18`

The first clean `npm test` run stopped on missing `data/spiritvale/acquisition/manifests.json`; subsequent validator runs identified the remaining minimum acquisition schemas/manifests and four source-registry test modules plus their four implementation dependencies. Only those 18 files were added, all non-secret and all required to execute the repository's declared validators. `scripts/validate-playaig-release-ui.mjs` is inherited unchanged from HEAD and is not counted as migrated.

No entity collection, route module or acquisition directory was silently expanded beyond this dependency finding. The additions remain review candidates and are not a baseline commit approval.

## Secret Firewall

`SECRET_FIREWALL=PASS`

Excluded from migration and absent from the reconstruction:

- `credentials/token.json`
- `credentials/client_secret.json`
- `credentials/`
- `.env`
- `.env.local`

The tracked HEAD `.env.production` file was inherited only because it is part of the baseline commit; its values were not printed or inspected. No credentials or tokens entered the reconstruction. No secret values were copied into reports.

## Build Results

### First clean reconstruction

| Command | Result |
|---|---|
| `npm ci` | PASS (173 packages installed; npm reported one audit vulnerability warning) |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `npm test` | PASS after the 18 dependency findings were migrated |

Build output generated 12 guide routes, 7 class routes, 7 database category routes, 50 card entity routes, 50 equipment entity routes, 52 monster entity routes, 20 skill entity routes and a 202-URL sitemap.

### Reproducibility copy

Temporary verification copy:

`/Users/cj/Documents/PlayAIG-Repro-Verification-20260817/`

It was copied only from the clean reconstruction (excluding `.git`, dependencies, caches, credentials, `dist-playground`, and generated sitemap/RSS), then independently ran `npm ci`, `npm run typecheck`, `npm run lint`, `npm run build` and `npm test`. All passed and regenerated a 202-URL sitemap.

## Data Parity

`DATA_PARITY=PASS`

| Collection | Original local | Reconstruction |
|---|---:|---:|
| Guides | 12 | 12 |
| Classes | 7 | 7 |
| Cards | 50 | 50 |
| Equipment | 50 | 50 |
| Monsters | 52 | 52 |
| Skills | 20 | 20 |
| Maps | 0 | 0 |

## Route Parity

`ROUTE_PARITY=PASS`

Verified static routes:

- `/`
- `/guides/`
- `/classes/`
- `/database/`
- `/database/maps/`
- `/guides/card-system-guide/`
- `/database/cards/`
- `/classes/knight/`
- `/classes/warrior/`

Entity route output was generated for 50 Cards, 50 Equipment, 52 Monsters and 20 Skills. A browser check also rendered `/database/cards/abomination-card/` with its verified source-backed content.

## SEO Parity

`SEO_PARITY=PASS`

- Public and dist sitemap counts: 202 URLs.
- `robots.txt`, `rss.xml`, `opensearch.xml` and `site.webmanifest` present in both public and dist outputs.
- Discovery files matched the original local generated outputs.
- The five Phase 1 target HTML outputs matched the original local outputs byte-for-byte.
- Target canonicals and titles use `https://playaig.com`.
- Static SEO generation emitted guide/class/database metadata and entity URLs without placeholder hostnames.

## Phase 1 Content Parity

`PHASE1_CONTENT_PARITY=PASS`

The five target routes were compared by generated HTML and browser DOM:

- `/database/maps/`
- `/guides/card-system-guide/`
- `/database/cards/`
- `/classes/knight/`
- `/classes/warrior/`

Each rendered one H1 with the expected title and canonical. The generated HTML for all five routes was identical to the original local build, preserving landing content, FAQ/JSON-LD, verification labels, source links and entity links.

## Browser Review

`CLEAN_BROWSER_REVIEW=PASS`

Local Vite review ran at `http://127.0.0.1:5173/`. The five Phase 1 routes, Database home and `/database/cards/abomination-card/` were navigated directly. Each returned the expected rendered content, one H1, title and `https://playaig.com` canonical. Fresh-tab console checks reported no runtime errors; the only persistent message was the expected warning that GA4 is disabled when `VITE_GA4_MEASUREMENT_ID` is absent in the local clean environment. A prior multi-route tab emitted one transient duplicate-key warning during route transitions; it did not reproduce in isolated final checks. No visual/browser failure blocked review.

## Reproducibility Test

`REPRODUCIBILITY=PASS`

The second clean verification copy was built solely from the reconstruction tree. It passed `npm ci`, typecheck, lint, build and the complete test suite, and independently regenerated the same 202-URL sitemap and entity route counts. No file was taken from the original dirty worktree during this gate.

## Reconstruction Delta Manifest

Path-level details are recorded in:

`docs/recovery/RECONSTRUCTION_DELTA_MANIFEST_V1.md`

Summary:

- 39 explicit manifest files migrated.
- 18 `UNKNOWN_BUT_REQUIRED` validator/test dependencies migrated after observed failures.
- 2 generated outputs regenerated, never used as source-of-truth.
- 0 hash mismatches among copied files.
- 0 secret files migrated.

## Original Worktree Integrity

`ORIGINAL_WORKTREE_PRESERVED=YES`

Before and after checks of `/Users/cj/Documents/SpiritVale站` showed:

- branch: `main`
- HEAD: `2284785c6ea87c82d3336083c8839eb7e830d20f`
- status entries: 118 before and 118 after

No original-worktree `git add`, commit, reset, clean, restore, checkout, stash, rebase, merge, push or deploy was performed. The only dependency install accidentally executed in the original directory wrote to ignored local `node_modules`; tracked status and HEAD were unchanged. No source, data, SEO or production file was altered there.

## Remaining Risks

- The Phase 1.3 manifest's declared candidate count (40) conflicts with its explicit enumerated count (39); this must be resolved before any baseline staging.
- The 18 unknown-but-required files are test/validator dependency candidates and still require human ownership approval.
- `.env.production` is inherited from HEAD and requires separate public-only configuration review before deployment.
- npm reported one high-severity audit vulnerability during `npm ci`; no dependency upgrade was made in this recovery phase.
- Local browser review cannot verify production deployment state or external analytics/GSC access.

## Commit Readiness

`COMMIT_READINESS=NOT_READY_FOR_COMMIT`

The reconstruction is technically reproducible, but the delta manifest, manifest count conflict and unknown-but-required dependency ownership require user review before a baseline commit. No staging was performed.

## Final Decision

All technical reconstruction gates passed. The appropriate next action is review of the reconstruction delta, not a commit or deployment.

`PHASE_STATUS=READY_FOR_BASELINE_COMMIT_REVIEW`  
`CLEAN_RECONSTRUCTION_STATUS=PASS`  
`CLEAN_RECONSTRUCTION_PATH=/Users/cj/Documents/PlayAIG-Clean-Reconstruction-20260817/`  
`RECONSTRUCTION_BRANCH=recovery/playaig-clean-baseline-20260817`  
`STARTING_HEAD=2284785c6ea87c82d3336083c8839eb7e830d20f`  
`MIGRATED_FILE_COUNT=57`  
`UNKNOWN_BUT_REQUIRED_COUNT=18`  
`SECRET_FIREWALL=PASS`  
`BUILD_STATUS=PASS`  
`DATA_PARITY=PASS`  
`ROUTE_PARITY=PASS`  
`SEO_PARITY=PASS`  
`PHASE1_CONTENT_PARITY=PASS`  
`CLEAN_BROWSER_REVIEW=PASS`  
`REPRODUCIBILITY=PASS`  
`RECONSTRUCTION_DELTA_FILE_COUNT=59`  
`ORIGINAL_WORKTREE_PRESERVED=YES`  
`COMMIT_READINESS=NOT_READY_FOR_COMMIT`  
`PUSH_STATUS=NOT_PERFORMED`  
`DEPLOYMENT_STATUS=NOT_PERFORMED`  
`RECOMMENDED_NEXT_PHASE=Human review of the reconstruction delta and manifest conflict`  
`NEXT_ACTION=Review and approve the delta manifest; do not commit, push or deploy until explicit approval.`
